{
  "name": "nanpure-web",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build": "tsc --pretty false",
    "typecheck": "tsc --noEmit --pretty false",
    "dev": "npm run build && python3 -m http.server 5173",
    "test": "npm run build && node --test src/core/sudoku.test.js"
  }
}
